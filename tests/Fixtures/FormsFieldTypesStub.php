<?php

/**
 * Forms field types probe stub.
 *
 * @package    ArtisanPack_UI
 * @subpackage Bookings
 *
 * @since      1.0.0
 */

declare( strict_types=1 );

namespace Tests\Fixtures;

/**
 * Stands in for `artisanpack-ui/forms`' field type configuration.
 *
 * The forms package is a `suggest`, so it is genuinely absent from this
 * package's test suite — which is what makes the closed branch of
 * {@see \ArtisanPackUI\Bookings\Support\HookSubscriptions} testable, and the
 * open one untestable without something to alias the probe name onto. This is
 * that something. It carries no behaviour because the gate only asks whether
 * the name is loadable.
 *
 * @package    ArtisanPack_UI
 * @subpackage Bookings
 *
 * @since      1.0.0
 */
class FormsFieldTypesStub
{
}
